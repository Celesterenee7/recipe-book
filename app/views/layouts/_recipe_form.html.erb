<%= render "layouts/errors", :object => @recipe %>

<%= form_for @recipe do |f| %>
  <h4><%= f.label :name %></h4>
  <%= f.text_field :name %>
  <h4><%= f.label :rating %></h4>
  <%= f.number_field :rating %>
  <h4><%= f.label :instruction %></h4>
  <%= f.text_area :instruction %>
  <p><%= f.submit :Submit, class: 'btn btn-primary'  %></p>
<% end %>
