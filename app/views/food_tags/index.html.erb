<h1>TAGS:</h1>
<% if @food_tags.any? %>
<ul>
  <% @food_tags.each do |tag| %>
  <li><%= link_to tag.name, food_tag_path(tag.id) %></li>
  <% end %>
</ul>
<% else %>
  <p>There are no tags yet.</p>
<% end %>
<p><%= link_to "Create new tag", new_food_tag_path %></p>
